**Fix the error!**

The function `add()` takes in a number, `x`, and adds x to the global variable `total`.

At the end of our code, we want to log the value of `total`, however, there is a bug in our code. Fix the bug so that we log out the proper total. 

**Hint:** Think about whether our `total` variable is in local or global scope? How does that affect how we use it in our function?